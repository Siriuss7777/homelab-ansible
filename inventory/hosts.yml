---
# No addresses here: every host name is an alias from ~/.ssh/config
# (HostName, User, Port are resolved by ssh itself).
all:
  children:
    k3s_cluster:
      children:
        k3s_masters:
          hosts:
            k3s-master:
        k3s_workers:
          hosts:
            k3s-worker:
